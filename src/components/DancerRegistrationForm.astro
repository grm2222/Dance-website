---
// Posts to our own endpoint only; the Sanity write token stays server-side.
---

<form
  id="dancer-form"
  method="post"
  action="/api/register-dancer"
  class="space-y-5"
  novalidate
>
  <div class="grid gap-5 sm:grid-cols-2">
    <div>
      <label for="lastName" class="block text-sm font-medium mb-1">
        Овог <span class="text-red-600" aria-hidden="true">*</span>
      </label>
      <input
        type="text"
        id="lastName"
        name="lastName"
        required
        maxlength="100"
        autocomplete="family-name"
        class="w-full rounded-lg border border-gray-300 px-3 py-3 focus:border-theme-600 focus:ring-2 focus:ring-theme-100 outline-none"
      />
    </div>
    <div>
      <label for="firstName" class="block text-sm font-medium mb-1">
        Нэр <span class="text-red-600" aria-hidden="true">*</span>
      </label>
      <input
        type="text"
        id="firstName"
        name="firstName"
        required
        maxlength="100"
        autocomplete="given-name"
        class="w-full rounded-lg border border-gray-300 px-3 py-3 focus:border-theme-600 focus:ring-2 focus:ring-theme-100 outline-none"
      />
    </div>
  </div>

  <div class="grid gap-5 sm:grid-cols-2">
    <div>
      <label for="birthDate" class="block text-sm font-medium mb-1">
        Төрсөн огноо <span class="text-red-600" aria-hidden="true">*</span>
      </label>
      <input
        type="date"
        id="birthDate"
        name="birthDate"
        required
        autocomplete="bday"
        class="w-full rounded-lg border border-gray-300 px-3 py-3 focus:border-theme-600 focus:ring-2 focus:ring-theme-100 outline-none"
      />
    </div>
    <fieldset>
      <legend class="block text-sm font-medium mb-1">
        Хүйс <span class="text-red-600" aria-hidden="true">*</span>
      </legend>
      <div class="flex gap-6 pt-2.5">
        <label class="flex items-center gap-2 text-sm">
          <input type="radio" name="gender" value="male" required class="accent-theme-600 h-4 w-4" />
          Эрэгтэй
        </label>
        <label class="flex items-center gap-2 text-sm">
          <input type="radio" name="gender" value="female" class="accent-theme-600 h-4 w-4" />
          Эмэгтэй
        </label>
      </div>
    </fieldset>
  </div>

  <div>
    <label for="club" class="block text-sm font-medium mb-1">Харьяалагдах клуб</label>
    <input
      type="text"
      id="club"
      name="club"
      maxlength="200"
      placeholder="Хэрэв клубт харьяалагддаг бол нэрийг нь бичнэ үү"
      class="w-full rounded-lg border border-gray-300 px-3 py-3 focus:border-theme-600 focus:ring-2 focus:ring-theme-100 outline-none"
    />
  </div>

  <div class="grid gap-5 sm:grid-cols-2">
    <div>
      <label for="phone" class="block text-sm font-medium mb-1">
        Утасны дугаар <span class="text-red-600" aria-hidden="true">*</span>
      </label>
      <input
        type="tel"
        id="phone"
        name="phone"
        required
        maxlength="30"
        autocomplete="tel"
        class="w-full rounded-lg border border-gray-300 px-3 py-3 focus:border-theme-600 focus:ring-2 focus:ring-theme-100 outline-none"
      />
    </div>
    <div>
      <label for="email" class="block text-sm font-medium mb-1">
        Имэйл хаяг <span class="text-red-600" aria-hidden="true">*</span>
      </label>
      <input
        type="email"
        id="email"
        name="email"
        required
        maxlength="200"
        autocomplete="email"
        class="w-full rounded-lg border border-gray-300 px-3 py-3 focus:border-theme-600 focus:ring-2 focus:ring-theme-100 outline-none"
      />
    </div>
  </div>

  <!-- Honeypot: hidden from humans, bots tend to fill it -->
  <div class="hidden" aria-hidden="true">
    <label for="dancer-website">Вэбсайт</label>
    <input type="text" id="dancer-website" name="website" tabindex="-1" autocomplete="off" />
  </div>

  <p
    id="dancer-form-status"
    role="status"
    aria-live="polite"
    tabindex="-1"
    class="hidden rounded-lg px-4 py-3 text-sm"
  >
  </p>

  <button
    type="submit"
    id="dancer-form-submit"
    class="w-full sm:w-auto rounded-lg bg-theme-600 px-8 py-3 font-semibold text-white hover:bg-theme-700 transition-colors disabled:opacity-60"
  >
    Хүсэлт илгээх
  </button>
</form>

<script>
  const form = document.getElementById('dancer-form') as HTMLFormElement | null;
  const status = document.getElementById('dancer-form-status');
  const submit = document.getElementById('dancer-form-submit') as HTMLButtonElement | null;

  function showStatus(message: string, ok: boolean) {
    if (!status) return;
    status.textContent = message;
    status.classList.remove('hidden', 'bg-green-50', 'text-green-800', 'bg-red-50', 'text-red-800');
    status.classList.add(ok ? 'bg-green-50' : 'bg-red-50', ok ? 'text-green-800' : 'text-red-800');
    status.scrollIntoView({ block: 'nearest', behavior: 'smooth' });
    status.focus({ preventScroll: true });
  }

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (!form.reportValidity()) return;
    if (submit) submit.disabled = true;

    try {
      const res = await fetch('/api/register-dancer', {
        method: 'POST',
        body: new FormData(form),
      });
      const data = (await res.json()) as { ok: boolean; error?: string };
      if (res.ok && data.ok) {
        form.reset();
        showStatus('Таны хүсэлтийг хүлээн авлаа. Бид тантай удахгүй холбогдох болно.', true);
      } else {
        showStatus(data.error ?? 'Алдаа гарлаа. Дахин оролдоно уу.', false);
      }
    } catch {
      showStatus('Сүлжээний алдаа гарлаа. Дахин оролдоно уу.', false);
    } finally {
      if (submit) submit.disabled = false;
    }
  });
</script>
